To run the generator with a GUI, run ascii_generator.py.

To run the generator with a command line interface, run generator.py.

Written in Python 3.9.0.

This was my first Python project done without reading a guide or copying an example from a text. The shading is fairly basic, but should work well in images with good contrast.
